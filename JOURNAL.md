# Standards Journal

只记录这套工程规范与知识体系本身的重大变化，最新在前。普通项目进度不进入这里。

## 2026-09

- **2026-09-04 — 增加 WorkBuddy HY4 capability routing：** Owner 长期可用的工程资源不再只按“普通 TeleAgent vs Codex”二选一。WorkBuddy HY4 被正式定义为高能力、quota 相对充裕的 execution / review backend，而不是新的跨项目固定 Role 或角色名字。以后 Project Manager Role 应根据 complexity、risk、ambiguity、blast radius、architecture depth、verification difficulty 与当前 quota/availability，在普通 Execution resources、HY4 等高能力工程资源和 Codex / Deep Engineering resources 之间动态选择。能机械结构化且可验证的任务仍优先普通 TeleAgent；中高复杂度、语义一致性、Release Truth、Project Memory、governance consistency、复杂 integration / recovery / Review 等任务优先考虑 HY4；最高风险架构、Contract、核心 runtime、重大跨 repo Core integration、极复杂 debugging 等真正 Deep Engineering 任务仍应使用 Codex 或相应深水资源。升级不是固定流水线，也不要求先失败一次。HY4 可做 Writer / Reviewer / specialist，但继续遵守 one Writer + read-only Reviewer 与显式 ownership transfer。工程师数量 0 / 1 / 2 规则保持不变，Reviewer 不为凑人数计为 Writer。新增派工透明度规则：Project Manager Role 每次给 Owner 正式工程 Prompt 前，必须先说明项目角色名、实际 Agent/backend、Task ID 与简短路由原因。
- **2026-09-04 — 明确 Agent workspace 与 Project workspace 边界：** Owner 当前使用的执行 Agent 都运行在本机，虽然各自拥有默认 workspace，但都能访问 `/Users/hwang/Movies/Program`。因此正式规则调整为：Agent 默认 workspace 只作为 Agent 产品自身的 session、缓存、工具状态等运行环境，不作为项目代码或项目事实存放位置；正式项目工程活动必须先切换到 `/Users/hwang/Movies/Program/<project-name>/` 的 Project workspace。已有正式 Project workspace 时，Agent 禁止在自己的 workspace 再 clone、复制或 `git init` 同一项目副本，避免 HEAD、dirty state、unpushed commit 和 remote 状态分叉。若 Agent workspace 已存在旧 clone，先检查未提交/未 push 成果并报告，再回到正式 Project workspace；不允许两个副本同时作为活跃工程 workspace。该规则已写入 `LOCAL-WORKSPACE-STANDARD.md` 并接入 Bootstrap 与 AGENTS 入口。
- **2026-09-04 — 建立统一 Local Workspace Standard：** 所有项目的本地统一根目录固定为 `/Users/hwang/Movies/Program`，一个项目对应其下一个项目子文件夹。单仓库项目可让项目目录本身作为 Git working tree；多仓库项目将全部相关 repo 收拢到同一项目目录。项目相关代码、文档、脚本、clone、worktree、测试产物与临时工程活动不得长期散落到项目目录之外；并行 worktree 也必须位于对应项目目录内部。正式施工前必须确认 `pwd`、repository、`origin`、branch/base SHA，防止在旧 clone、错误 clone 或错误项目目录施工。需要长期保留的工程成果必须进入对应 Git repo 并 commit/push，GitHub remote 继续作为 canonical truth。新增 `LOCAL-WORKSPACE-STANDARD.md` 并接入 README、Bootstrap、AGENTS、Engineering Standards 与 Git/GitHub Standard。
- **2026-09-04 — 角色命名改为项目级：** 全局规范不再固定任何项目角色名字，只固定职责类型：Project Manager Role、Deep Engineering Role、Primary Execution Role、Secondary Execution Role、Owner。每个项目应维护自己的角色命名映射；已有项目沿用既有名称，新项目没有映射时由 Project Manager Role 自动生成一组项目专属、易区分的名字并向 Owner 报告。正式 Prompt、handoff、project state 与工程师配置优先使用项目角色名。任务生命周期的全局 Review 语义改用 `PM_REVIEW`；项目可显示为 `<Project PM Name>_REVIEW`。历史记录中曾出现的角色名字仅代表当时的旧全局命名或具体项目别名，不再是当前全局默认。
- **2026-09-01 — 增加 restricted-content hard gate：** Owner 新增跨项目不可放宽的内容禁令。为避免规范仓库本身出现禁用名称，使用 Unicode code-point sequence `U+6E56 U+5317 U+7535 U+4FE1` 标识受限组织，并禁止其英文直译、英文品牌写法、拼音、缩写、中英混合及其他可直接识别为同一组织的等价表达。规则覆盖源码、注释、文档、测试、fixture、UI、日志、Prompt/handoff、Git metadata、release、artifact 等所有项目可控内容；外部输入命中时必须先替换为中性名称。该规则接入新会话 Bootstrap、Agent 必读链路和 Definition of Done，任何命中都必须 `NEEDS_CORRECTION`，不得 `PASS`。
- **2026-09-01 — 从 DeepTalk 反向沉淀正式任务机制：** 将已经在复杂 multi-repo 项目中验证成熟的三类做法升级为跨项目 Standard：①正式/跨会话/可重复派发任务使用唯一 Task ID，并区分 Prompt 草稿、发送、运行、push、Review、ACCEPTED 等真实状态；发送状态不明时使用 `SEND_STATUS_UNKNOWN` + `STATUS_PROBE_ONLY`，不得直接重发；②Task ID 同时作为幂等键，Agent 执行前必须检查已有结果，同一任务已完成时返回 `ALREADY_COMPLETED`；③当项目经理能访问目标 GitHub 时默认使用 GitHub-native handoff，Owner 只需报告 Agent + Task ID 完成，项目经理自行核验 branch、exact SHA、diff、源码与 CI，完整人工 handoff 降级为兜底机制。项目结构特有的多线并行策略没有机械升级为全局默认。
- **2026-09-01 — 增加新会话启动语提醒：** 当项目经理判断应为新项目开独立 ChatGPT 会话、需要 handoff 到全新会话、或为了隔离上下文建议新开会话时，必须主动向 Owner 提供可直接复制的启动语：`请参考 https://github.com/HWang0310/engineering-journal 的工程规范，开发 XXX 项目。` 已知项目名时直接替换 `XXX`；已知项目仓库时可一并附上。
- **2026-09-01 — 增加工程师数量决策规则：** 项目经理在新项目、阶段切换、重大阻塞或依赖变化时，必须重新判断需要 0 / 1 / 2 名执行工程师，并向 Owner 明确报告本阶段工程师配置。默认 1 名 Writer；只有真正独立、无写入冲突、可分别验收时才并行；Deep Engineering Role 按需专项介入。
- **2026-09-01 — 新会话启动协议建立：** 新增 `NEW-SESSION-BOOTSTRAP.md`，并把 README / AGENTS 变成显式入口。以后用户只需说“请参考 engineering-journal 的工程规范，开发 XXX 项目”，新 ChatGPT / Agent 会话就应先读取规定文件、确认 remote 基线、自动进入项目经理职责模式，并继承 Agent 路由、Prompt、并行、Git、exact-SHA Review 和 PASS/HOLD/NEEDS_CORRECTION 等规则。
- **2026-09-01 — 仓库重新定位为跨项目工程规范库：** 从“个人开发月度日志/项目状态模板”转为长期 engineering standards + reusable knowledge，沉淀角色职责模型、多 Agent 并行边界、GitHub remote/exact-SHA 事实规则、一步一验收、Prompt/Handoff 规范以及质量优先的能力路由原则。

## 2026-08

- **2026-08-09 — 初始仓库建立：** 创建 `engineering-journal`，最初用于保存个人开发计划、项目记录与决策。该定位已于 2026-09-01 被更聚焦的“跨项目工程规范与知识积累库”取代。